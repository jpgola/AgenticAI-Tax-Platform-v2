
# AgenticAI Frontend (Vite + React)

## Run locally
```bash
npm install
npm run dev
```

This frontend proxies `/api/*` to `http://127.0.0.1:8000` in dev via Vite config.

## Build
```bash
npm run build
npm run preview
```
